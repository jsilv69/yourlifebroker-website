# Files NOT to upload as static assets (gitignore syntax).
# The static site is index.html, css/, js/, assets/, plans/, robots.txt, sitemap.xml.

# Build/runtime junk
node_modules/
.git/

# Node/Fly backend + deploy config (the API runs on Fly, not Cloudflare)
server.js
package.json
package-lock.json
Dockerfile
fly.toml

# Cloudflare + tooling config
wrangler.jsonc
.assetsignore
.dockerignore
.gitignore

# Docs & secrets
README.md
.env
.env.*
